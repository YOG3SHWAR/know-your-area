---
phase: 01-core-capture-to-feed-skeleton
plan: 07
type: execute
wave: 2
depends_on: ["01-06"]
files_modified: []
autonomous: false
gap_closure: true
gap_ids: [G-01-EXTRA-1]
requirements: [FEED-01]

user_setup:
  - service: vercel + supabase
    why: "The production /api/feed 500 root cause is an env/config question no code change can decide — it needs Vercel function-log access and the DATABASE_URL/pooler configuration in the Vercel Production environment scope."
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard → Project Settings → Database → Connection string → Session pooler (or Transaction pooler); set in Vercel → Project → Settings → Environment Variables, scoped to Production"
    dashboard_config:
      - task: "Read the actual thrown error for GET /api/feed"
        location: "Vercel → Project → Logs (Functions) — filter to /api/feed 500s after redeploy"
      - task: "Confirm DATABASE_URL uses Supabase's Session/Transaction POOLER host (IPv4-reachable, e.g. *.pooler.supabase.com:6543/5432), not the direct db.<ref>.supabase.co host (IPv6-only; Vercel serverless has no IPv6 egress)"
        location: "Supabase Dashboard → Project Settings → Database → Connection string"
      - task: "Confirm the Supabase project is not paused (free tier pauses after 7 days of inactivity)"
        location: "Supabase Dashboard → Project home"

must_haves:
  truths:
    - "https://know-your-area.vercel.app/ loads real complaint data on a normal page load — GET /api/feed returns HTTP 200 with a JSON feed page, not HTTP 500, and the home feed shows reports (or the legitimate empty state) rather than the 'Couldn't load reports…' error banner."
  artifacts: []
  key_links:
    - "Vercel Production DATABASE_URL → Supabase Session/Transaction pooler connection string is what makes serverless feed queries reach the database (IPv4 + TLS + pooler), turning the plan-01-06 code fix into a working production feed."
---

<objective>
Complete the closure of **G-01-EXTRA-1** that code alone cannot: verify the production feed actually loads after the plan-01-06 client hardening is deployed, and confirm/repair the env-level configuration (DATABASE_URL pooler host, Vercel Production scope, Supabase not paused) using the Vercel logs and dashboards that the investigating agent could not access.

Purpose: the ranked hypotheses for the 500 (IPv6-only direct host vs. missing TLS vs. transaction-pooler prepared statements vs. unset/paused DB) can only be disambiguated against the real error log and the actual connection string — both live behind the Vercel/Supabase dashboards.
Output: a confirmed-green production feed (or a precisely-identified remaining config fix), recorded in the plan summary.

Depends on plan **01-06** (the postgres.js `ssl`/`prepare:false` fix and the feed-route error logging) being merged and deployed to the Vercel Production deployment under test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
# The diagnosed gap, ranked hypotheses, and the exact non-code follow-ups:
@.planning/phases/01-core-capture-to-feed-skeleton/01-UAT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify production feed loads (DATABASE_URL pooler + redeploy)</name>
  <what-built>
    Plan 01-06 added explicit `ssl:"require"` (for hosted hosts) and `prepare:false` to the postgres.js client and made `GET /api/feed` log the real error (name/message/code) server-side instead of swallowing it. Those are the code-level defensive fixes for the production feed 500. What remains is env/config verification that no code change can perform — it needs your Vercel + Supabase dashboard access.
  </what-built>
  <how-to-verify>
    1. Ensure the plan-01-06 fix is deployed: merge/push so Vercel builds a new Production deployment (or trigger a redeploy).
    2. In the Supabase Dashboard → Project Settings → Database → Connection string, copy the **Session pooler** (or Transaction pooler) connection string — the host looks like `*.pooler.supabase.com` on port 6543/5432. Do NOT use the direct `db.<ref>.supabase.co` host: Vercel serverless functions have no IPv6 egress and that direct host is IPv6-only (hypothesis 1, the most plausible cause).
    3. In Vercel → Project → Settings → Environment Variables, set/confirm `DATABASE_URL` to that pooler string and confirm it is scoped to **Production** (hypothesis 4). Redeploy if you changed it.
    4. Confirm the Supabase project is not paused (free tier pauses after 7 days inactivity) — Supabase Dashboard → Project home.
    5. If it still 500s, open Vercel → Project → Logs (Functions), filter to the `/api/feed` 500, and read the now-surfaced error name/message/code (from the 01-06 logging) to identify which remaining hypothesis is true (e.g. a TLS handshake error, an IPv6/ENETUNREACH connect error, or a prepared-statement error).
    6. Confirm success two ways:
       - `curl -i https://know-your-area.vercel.app/api/feed` returns **HTTP 200** with a JSON body containing an `items` array (not `{"error":"Couldn't load reports."}` with status 500).
       - Load `https://know-your-area.vercel.app/` in a browser: the feed shows real reports (or the legitimate "No reports near you yet" empty state), NOT the red "Couldn't load reports…" banner.
  </how-to-verify>
  <resume-signal>Type "approved" once GET /api/feed returns 200 in production and the home feed loads, or describe the surfaced error (name/message/code from the Vercel log) if it still fails so the remaining config fix can be planned.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Vercel/Supabase secrets | DATABASE_URL is a credential; it must live only in Vercel's encrypted env store, never committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Information Disclosure | DATABASE_URL secret | medium | mitigate | Set DATABASE_URL only via Vercel's Environment Variables (Production scope) and the Supabase pooler string with TLS; never paste it into code, commits, or logs. |
</threat_model>

<verification>
- `curl -i https://know-your-area.vercel.app/api/feed` → HTTP 200 with a JSON `items` array.
- `https://know-your-area.vercel.app/` renders the feed (real reports or the empty state), not the error banner.
</verification>

<success_criteria>
- G-01-EXTRA-1 closed in production: the public feed loads real data on a normal page load; `/api/feed` returns 200, not 500.
- The confirmed root cause (which hypothesis was true) and the applied config fix are recorded in the plan summary for the milestone record.
</success_criteria>

<output>
Create `.planning/phases/01-core-capture-to-feed-skeleton/01-07-SUMMARY.md` when done.
</output>
