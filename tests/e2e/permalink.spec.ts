import { test } from "./fixtures";

// FEED-04: each complaint has a shareable, crawlable permalink at
// /c/{id}. Replaced with real assertions in Plan 04 (feed slice).
test.fixme("permalink page: renders the complaint at /c/{id} (FEED-04)", async () => {});
