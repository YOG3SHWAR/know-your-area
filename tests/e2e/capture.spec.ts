import { test } from "./fixtures";

// SUBM-01: camera-only capture (no gallery/file-picker path exists).
// SUBM-03: live GPS is read from the browser at submission time, not EXIF.
// Replaced with real assertions in Plan 03 (capture slice).
test.fixme(
  "capture flow: live camera + GPS produces a published complaint (SUBM-01, SUBM-03)",
  async () => {},
);
