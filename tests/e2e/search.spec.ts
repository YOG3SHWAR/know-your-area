import { test } from "./fixtures";

// FEED-03: user can search for a complaint by its opaque ID and jump
// straight to its permalink. Replaced with real assertions in Plan 04
// (feed slice).
test.fixme("search by ID: redirects to the matching complaint's permalink (FEED-03)", async () => {});
