import { test } from "./fixtures";

// FEED-01: landing page shows a feed of complaints near the visitor's
// current location, distance-sorted. Replaced with real assertions in
// Plan 04 (feed slice).
test.fixme("feed page: shows nearby complaints distance-sorted (FEED-01)", async () => {});
